# ClawDrip Environment Configuration

# Database (Supabase PostgreSQL)
DATABASE_URL=postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres

# Server
PORT=3001
NODE_ENV=development  # Set to 'production' on Vercel
CORS_ORIGIN=http://localhost:9000

# Wallet (receives USDC payments)
WALLET_ADDRESS=0xd9baf332b462a774ee8ec5ba8e54d43dfaab7093

# x402 Configuration (v2.3 — Base + Solana)
X402_FACILITATOR_URL=https://facilitator.x402.org
X402_NETWORK=base-sepolia  # Use 'base' for mainnet (CAIP-2: eip155:8453)
X402_SVM_NETWORK=solana-devnet  # Use 'solana' for mainnet

# Solana wallet (receives USDC payments on Solana — optional)
SOLANA_WALLET_ADDRESS=

# Gift Wallet Encryption — 32-byte hex key for AES-256-GCM encryption of gift wallet private keys
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
GIFT_WALLET_ENCRYPTION_KEY=

# Resend — API key for transactional emails (order confirmations on claim)
# Get one at https://resend.com
RESEND_API_KEY=

# Email — Sender address for order confirmation emails
FROM_EMAIL=orders@clawdrip.com

# AI Design Generation (Gemini 2.5 Flash Image — "Nano Banana")
# Required for real AI design generation ($0.039/image)
# Get one at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Frontend (Vite)
VITE_API_URL=http://localhost:3001
